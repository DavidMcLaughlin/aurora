import React from 'react';

const ConfigGroups = ({ groups }) => {
  return (<div className='instanceGroups'>
    <div className='groupSummary' style={{width: '100%', 'background-color': 'steelblue'}}>
      <a className='groupLabel' href="#">0-5</a>
    </div>
  </div>);
};

export default ConfigGroups;
