import React from 'react';

/*
   An example of how to replace React components in src/main/js for custom Scheduler
   UI widgets, etc. Drop replacements files in here and that component will be used
   instead.
 */
export default () => <div>Hello, Pluggable World!</div>;
